/* POO */
// Programación orientada a objetos

//Generar una representación abstracta de la vida real (paramtrizar objetos de la vida real para definirlo)


var coche = {
    // propiedades del objeto
    marca : "Toyota", 
    modelo : "C-HR",
    anyo : 2020,
    // métodos de un objeto (es cualquier función que esté dentro de un objeto)
    detalles : function  () {
        console.log(` ${this.marca}: ${this.modelo} \\ ${this.anyo} `);

    }

};

coche.marca; // así se llama a una propiedad del objeto

coche.detalles() // así se llama a un método del objeto


var botellaAgua = {
    altura : ,
    marca : ,
    contenido : ,
    cantidad : , 
    color : ,
    tipoTapon :       
}

